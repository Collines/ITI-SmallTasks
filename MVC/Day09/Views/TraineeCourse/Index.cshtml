@model IEnumerable<Day09_remake.Models.TraineeCourse>
@using Day09_remake.Models
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Course)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Trainee)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Grade)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Course.Topic)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Trainee.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Grade)
                </td>
            <td>
                    @Html.ActionLink("Edit", "Edit",new TCID(){TraineeID=item.TraineeID,CourseID=item.CourseID}) |
                    @Html.ActionLink("Details", "Details",new TCID(){TraineeID=item.TraineeID,CourseID=item.CourseID}) |
                    @Html.ActionLink("Delete", "Delete", new TCID(){TraineeID=item.TraineeID,CourseID=item.CourseID})


                    @*@Html.ActionLink("Edit", "Edit", new { id= new {TraineeID=item.TraineeID,CourseID=item.CourseID} }) |
                    @Html.ActionLink("Details", "Details", new {id= new {TraineeID=item.TraineeID,CourseID=item.CourseID} }) |
                @Html.ActionLink("Delete", "Delete", new { id= new {TraineeID=item.TraineeID,CourseID=item.CourseID} })*@
            </td>
        </tr>
}
    </tbody>
</table>
